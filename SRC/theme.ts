// Central theme constants used across the app
export const COLORS = {
  primary: '#FFD700',
  black: '#000000',
  white: '#FFFFFF',
  gray: '#F2F2F2',
  text: '#333333',
};

export const SIZES = {
  padding: 20,
  radius: 10,
  title: 22,
  text: 16,
};

// helper types for TypeScript
export type ThemeColors = typeof COLORS;
export type ThemeSizes = typeof SIZES;
